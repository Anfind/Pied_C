#include<stdio.h>
#include<math.h>
//
int main(){
    

    return 0;
}
/*
int start, end, sum= 0;
    scanf("%d%d", &start, &end);

    for(int num = start; num <= end; num++){
        int flag= 1;
        if(num >= 2){
            for(int i = 2; i <= sqrt(num); i++){
                if(num % i == 0) flag = 0;
                break;
            }
        }
        if( flag == 1){
            sum += num;
        }
    }
    printf("\n%d", sum);
    */